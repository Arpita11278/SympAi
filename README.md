# Virtual Disease Predictor (Streamlit)

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```

Replace CSVs inside `sample_datasets/` with your own larger dataset (same column names).
